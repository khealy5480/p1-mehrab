{
  "name": "frontend",
  "version": "1.0.0",
  "description": "Frontend for music app",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "build:live": "nodemon --watch 'src/**/*.ts' --exec \"ts-node\" src/main.ts",
    "start": "npm run build:live"
  },
  "author": "Mehrab Rahman",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.21",
    "typescript": "^4.6.2"
  }
}
